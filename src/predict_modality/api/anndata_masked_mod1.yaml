type: file
description: "masked dataset, training cells."
example: "masked_mod1.h5ad"
info:
  short_description: "masked data"
  slots:
    layers: 
      - type: integer
        name: counts
        description: Raw counts
        required: true
    obs:
      - type: string
        name: batch
        description: Batch information
        required: true
      - type: double
        name: size_factors
        description:
        required: false
    var:
      - type: string
        name: feature_types
        description: 
        required: true
      - type: string
        name: gene_ids
        description:
        required: false
    uns:
      - type: string
        name: dataset_id
        description: "A unique identifier for the dataset"
        required: true
      - type: string
        name: organism
        description: which organism
        required: true
    obsm:
      - type: double
        name: gene_activity
        description: gene activity
        required: false

