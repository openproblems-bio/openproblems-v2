type: file
description: "The solution for the data"
example: "solution.h5ad"
info:
  short_description: "Solution"
  lots:
    layers:
      - type: integer
        name: counts
        description: Raw counts
        required: false
    obs:
      - type: string
        name: batch
        description: Batch information
        required: false
      - type: double
        name: size_factors
        description:
        required: false
    var:
      - type: string
        name: feature_types
        description: 
        required: false
      - type: string
        name: gene_ids
        description:
        required: false
    uns:
      - type: string
        name: dataset_id
        description: "A unique identifier for the dataset"
        required: true
      - type: string
        name: organism
        description: which organism
        required: true
    obsm:
      - type: double
        name: gene_activity
        description: gene activity
        required: false
