functionality:
  name: "scprep_csv"
  namespace: "modality_alignment/datasets"
  version: "main_build"
  authors:
  - name: "Scott Gigante"
    roles:
    - "maintainer"
    - "author"
    props:
      github: "scottgigante"
  arguments:
  - type: "string"
    name: "--id"
    alternatives: []
    description: "The id of the output dataset id"
    default: "citeseq_cbmc"
    required: false
    direction: "Input"
    multiple: false
    multiple_sep: ":"
  - type: "file"
    name: "--input1"
    alternatives: []
    description: "Path or URL to the RNA counts as a gzipped csv file."
    default: "https:/www.ncbi.nlm.nih.gov/geo/download/?acc=GSE100866&format=file&file=GSE100866%5FCBMC%5F8K%5F13AB%5F10X%2DRNA%5Fumi%2Ecsv%2Egz"
    must_exist: false
    required: false
    direction: "Input"
    multiple: false
    multiple_sep: ":"
  - type: "file"
    name: "--input2"
    alternatives: []
    description: "Path or URL to the ADT counts as a gzipped csv file."
    default: "https:/www.ncbi.nlm.nih.gov/geo/download/?acc=GSE100866&format=file&file=GSE100866%5FCBMC%5F8K%5F13AB%5F10X%2DADT%5Fumi%2Ecsv%2Egz"
    must_exist: false
    required: false
    direction: "Input"
    multiple: false
    multiple_sep: ":"
  - type: "string"
    name: "--compression"
    alternatives: []
    description: "For on-the-fly decompression of on-disk data. If 'infer' and filepath_or_buffer\
      \ is path-like, then detect compression from the following extensions: '.gz',\
      \ '.bz2', '.zip', or '.xz' (otherwise no decompression). If using 'zip', the\
      \ ZIP file must contain only one data file to be read in. Set to None for no\
      \ decompression."
    default: "gzip"
    required: false
    direction: "Input"
    multiple: false
    multiple_sep: ":"
  - type: "boolean_true"
    name: "--test"
    alternatives: []
    description: "Subset the dataset"
    direction: "Input"
  - type: "file"
    name: "--output"
    alternatives:
    - "-o"
    description: "Output h5ad file containing both input matrices data"
    default: "output.h5ad"
    must_exist: false
    required: true
    direction: "Output"
    multiple: false
    multiple_sep: ":"
  resources:
  - type: "bash_script"
    text: |
      #!/usr/bin/env bash
      
      ###############################
      #    scprep_csv main_build    #
      ###############################
      
      # This wrapper script is auto-generated by viash 0.5.0-rc2 and is thus a
      # derivative work thereof. This software comes with ABSOLUTELY NO WARRANTY from
      # Data Intuitive.  The component may contain files which fall under a different
      # license. The authors of this component should specify the license in the
      # header of such files, or include a separate license file detailing the
      # licenses of all included files.
      #
      # Component authors:
      # * Scott Gigante (maintainer, author) {github: scottgigante}
      
      set -e
      
      if [ -z "$VIASH_TEMP" ]; then
        VIASH_TEMP=/tmp
      fi
      
      # define helper functions
      # ViashQuote: put quotes around non flag values
      # $1     : unquoted string
      # return : possibly quoted string
      # examples:
      #   ViashQuote --foo      # returns --foo
      #   ViashQuote bar        # returns 'bar'
      #   Viashquote --foo=bar  # returns --foo='bar'
      function ViashQuote {
        if [[ "$1" =~ ^-+[a-zA-Z0-9_\-]+=.+$ ]]; then
          echo "$1" | sed "s#=\(.*\)#='\1'#"
        elif [[ "$1" =~ ^-+[a-zA-Z0-9_\-]+$ ]]; then
          echo "$1"
        else
          echo "'$1'"
        fi
      }
      # ViashRemoveFlags: Remove leading flag
      # $1     : string with a possible leading flag
      # return : string without possible leading flag
      # examples:
      #   ViashRemoveFlags --foo=bar  # returns bar
      function ViashRemoveFlags {
        echo "$1" | sed 's/^--*[a-zA-Z0-9_\-]*=//'
      }
      # ViashSourceDir: return the path of a bash file, following symlinks
      # usage   : ViashSourceDir ${BASH_SOURCE[0]}
      # $1      : Should always be set to ${BASH_SOURCE[0]}
      # returns : The absolute path of the bash file
      function ViashSourceDir {
        SOURCE="$1"
        while [ -h "$SOURCE" ]; do
          DIR="$( cd -P "$( dirname "$SOURCE" )" >/dev/null 2>&1 && pwd )"
          SOURCE="$(readlink "$SOURCE")"
          [[ $SOURCE != /* ]] && SOURCE="$DIR/$SOURCE"
        done
        cd -P "$( dirname "$SOURCE" )" >/dev/null 2>&1 && pwd
      }
      VIASH_VERBOSITY=5
      
      # see https://en.wikipedia.org/wiki/Syslog#Severity_level
      
      # ViashLog: Log events depending on the verbosity level
      # usage: ViashLog 1 alert Oh no something went wrong!
      # $1: required verbosity level
      # $2: display tag
      # $3+: messages to display
      # stdout: Your input, prepended by '[$2] '. 
      function ViashLog {
        local required_level="$1"
        local display_tag="$2"
        shift 2
        if [ $VIASH_VERBOSITY -ge $required_level ]; then
          echo "[$display_tag]" "$@"
        fi
      }
      
      # ViashEmergency: log events when the system is unstable
      # usage: ViashEmergency Oh no something went wrong.
      # stdout: Your input, prepended by '[emergency] '. 
      function ViashEmergency {
        ViashLog 0 emergency $@
      }
      
      # ViashAlert: log events when actions must be taken immediately (e.g. corrupted system database)
      # usage: ViashAlert Oh no something went wrong.
      # stdout: Your input, prepended by '[alert] '. 
      function ViashAlert {
        ViashLog 1 alert $@
      }
      
      # ViashCritical: log events when a critical condition occurs
      # usage: ViashCritical Oh no something went wrong.
      # stdout: Your input, prepended by '[critical] '. 
      function ViashCritical {
        ViashLog 2 critical $@
      }
      
      # ViashError: log events when an error condition occurs
      # usage: ViashError Oh no something went wrong.
      # stdout: Your input, prepended by '[error] '. 
      function ViashError {
        ViashLog 3 error $@
      }
      
      # ViashWarning: log potentially abnormal events
      # usage: ViashWarning Something may have gone wrong.
      # stdout: Your input, prepended by '[warning] '. 
      function ViashWarning {
        ViashLog 4 warning $@
      }
      
      # ViashNotice: log significant but normal events
      # usage: ViashNotice This just happened.
      # stdout: Your input, prepended by '[notice] '. 
      function ViashNotice {
        ViashLog 5 notice $@
      }
      
      # ViashInfo: log normal events
      # usage: ViashInfo This just happened.
      # stdout: Your input, prepended by '[info] '. 
      function ViashInfo {
        ViashLog 6 info $@
      }
      
      # ViashDebug: log all events, for debugging purposes
      # usage: ViashDebug This just happened.
      # stdout: Your input, prepended by '[debug] '. 
      function ViashDebug {
        ViashLog 7 debug $@
      }
      
      # find source folder of this component
      VIASH_RESOURCES_DIR=`ViashSourceDir ${BASH_SOURCE[0]}`
      
      
      # ViashHelp: Display helpful explanation about this executable
      function ViashHelp {
         echo "scprep_csv main_build"
      echo "Create a modality alignment dataset from CSV using scprep."
         echo
         echo "Options:"
      
      echo "    --id"
      echo "        type: string"
      echo "        default: citeseq_cbmc"
      echo "        The id of the output dataset id"
      echo ""
      
      
      echo "    --input1"
      echo "        type: file"
      echo "        default: https:/www.ncbi.nlm.nih.gov/geo/download/?acc=GSE100866&format=file&file=GSE100866%5FCBMC%5F8K%5F13AB%5F10X%2DRNA%5Fumi%2Ecsv%2Egz"
      echo "        Path or URL to the RNA counts as a gzipped csv file."
      echo ""
      
      
      echo "    --input2"
      echo "        type: file"
      echo "        default: https:/www.ncbi.nlm.nih.gov/geo/download/?acc=GSE100866&format=file&file=GSE100866%5FCBMC%5F8K%5F13AB%5F10X%2DADT%5Fumi%2Ecsv%2Egz"
      echo "        Path or URL to the ADT counts as a gzipped csv file."
      echo ""
      
      
      echo "    --compression"
      echo "        type: string"
      echo "        default: gzip"
      echo "        For on-the-fly decompression of on-disk data. If 'infer' and filepath_or_buffer is path-like, then detect compression from the following extensions: '.gz', '.bz2', '.zip', or '.xz' (otherwise no decompression). If using 'zip', the ZIP file must contain only one data file to be read in. Set to None for no decompression."
      echo ""
      
      
      echo "    --test"
      echo "        type: boolean_true"
      echo "        Subset the dataset"
      echo ""
      
      
      echo "    -o, --output"
      echo "        type: file, required parameter, output"
      echo "        default: output.h5ad"
      echo "        Output h5ad file containing both input matrices data"
      echo ""
      
      }
      
      # initialise array
      VIASH_POSITIONAL_ARGS=''
      
      while [[ $# -gt 0 ]]; do
          case "$1" in
              -h|--help)
                  ViashHelp
                  exit
                  ;;
              -v|--verbose)
                  let "VIASH_VERBOSITY=VIASH_VERBOSITY+1"
                  shift 1
                  ;;
              -vv)
                  let "VIASH_VERBOSITY=VIASH_VERBOSITY+2"
                  shift 1
                  ;;
              --verbosity)
                  VIASH_VERBOSITY="$2"
                  shift 2
                  ;;
              --verbosity=*)
                  VIASH_VERBOSITY="$(ViashRemoveFlags "$1")"
                  shift 1
                  ;;
              --version)
                  echo "scprep_csv main_build"
                  exit
                  ;;
              --id)
                  VIASH_PAR_ID="$2"
                  shift 2
                  ;;
              --id=*)
                  VIASH_PAR_ID=$(ViashRemoveFlags "$1")
                  shift 1
                  ;;
              --input1)
                  VIASH_PAR_INPUT1="$2"
                  shift 2
                  ;;
              --input1=*)
                  VIASH_PAR_INPUT1=$(ViashRemoveFlags "$1")
                  shift 1
                  ;;
              --input2)
                  VIASH_PAR_INPUT2="$2"
                  shift 2
                  ;;
              --input2=*)
                  VIASH_PAR_INPUT2=$(ViashRemoveFlags "$1")
                  shift 1
                  ;;
              --compression)
                  VIASH_PAR_COMPRESSION="$2"
                  shift 2
                  ;;
              --compression=*)
                  VIASH_PAR_COMPRESSION=$(ViashRemoveFlags "$1")
                  shift 1
                  ;;
              --test)
                  VIASH_PAR_TEST=true
                  shift 1
                  ;;
              --output)
                  VIASH_PAR_OUTPUT="$2"
                  shift 2
                  ;;
              --output=*)
                  VIASH_PAR_OUTPUT=$(ViashRemoveFlags "$1")
                  shift 1
                  ;;
              -o)
                  VIASH_PAR_OUTPUT="$2"
                  shift 2
                  ;;
              *)  # positional arg or unknown option
                  # since the positional args will be eval'd, can we always quote, instead of using ViashQuote
                  VIASH_POSITIONAL_ARGS="$VIASH_POSITIONAL_ARGS '$1'"
                  shift # past argument
                  ;;
          esac
      done
      
      # parse positional parameters
      eval set -- $VIASH_POSITIONAL_ARGS
      
      
      
      # check whether required parameters exist
      if [ -z "$VIASH_PAR_OUTPUT" ]; then
        ViashError '--output' is a required argument. Use "--help" to get more information on the parameters.
        exit 1
      fi
      if [ -z "$VIASH_PAR_ID" ]; then
        VIASH_PAR_ID="citeseq_cbmc"
      fi
      if [ -z "$VIASH_PAR_INPUT1" ]; then
        VIASH_PAR_INPUT1="https:/www.ncbi.nlm.nih.gov/geo/download/?acc=GSE100866&format=file&file=GSE100866%5FCBMC%5F8K%5F13AB%5F10X%2DRNA%5Fumi%2Ecsv%2Egz"
      fi
      if [ -z "$VIASH_PAR_INPUT2" ]; then
        VIASH_PAR_INPUT2="https:/www.ncbi.nlm.nih.gov/geo/download/?acc=GSE100866&format=file&file=GSE100866%5FCBMC%5F8K%5F13AB%5F10X%2DADT%5Fumi%2Ecsv%2Egz"
      fi
      if [ -z "$VIASH_PAR_COMPRESSION" ]; then
        VIASH_PAR_COMPRESSION="gzip"
      fi
      if [ -z "$VIASH_PAR_TEST" ]; then
        VIASH_PAR_TEST="false"
      fi
      
      
      cat << VIASHEOF | bash
      set -e
      tempscript=\$(mktemp "$VIASH_TEMP/viash-run-scprep_csv-XXXXXX")
      function clean_up {
        rm "\$tempscript"
      }
      trap clean_up EXIT
      cat > "\$tempscript" << 'VIASHMAIN'
      # The following code has been auto-generated by Viash.
      par = {
        'id': $( if [ ! -z ${VIASH_PAR_ID+x} ]; then echo "'$VIASH_PAR_ID'"; else echo None; fi ),
        'input1': $( if [ ! -z ${VIASH_PAR_INPUT1+x} ]; then echo "'$VIASH_PAR_INPUT1'"; else echo None; fi ),
        'input2': $( if [ ! -z ${VIASH_PAR_INPUT2+x} ]; then echo "'$VIASH_PAR_INPUT2'"; else echo None; fi ),
        'compression': $( if [ ! -z ${VIASH_PAR_COMPRESSION+x} ]; then echo "'$VIASH_PAR_COMPRESSION'"; else echo None; fi ),
        'test': $( if [ ! -z ${VIASH_PAR_TEST+x} ]; then echo "'$VIASH_PAR_TEST'.lower() == 'true'"; else echo None; fi ),
        'output': $( if [ ! -z ${VIASH_PAR_OUTPUT+x} ]; then echo "'$VIASH_PAR_OUTPUT'"; else echo None; fi )
      }
      
      resources_dir = '$VIASH_RESOURCES_DIR'
      
      ## VIASH START
      ## VIASH END
      
      print("Importing libraries")
      import scprep
      
      # adding resources dir to system path
      # the resources dir contains all files listed in the '.functionality.resources' part of the
      # viash config, amongst which is the 'utils.py' file we need.
      import sys
      sys.path.append(resources_dir)
      
      # importing helper functions from common utils.py file in resources dir
      from utils import create_joint_adata
      from utils import filter_joint_data_empty_cells
      from utils import subset_joint_data
      
      print("Downloading expression datasets from GEO (this might take a while)") 
      sys.stdout.flush()
      
      # par["input1"] can be the path to a local file, or a url
      adata1 = scprep.io.load_csv(
          par["input1"], cell_axis="col", compression=par["compression"], sparse=True, chunksize=1000
      )
      adata2 = scprep.io.load_csv(
          par["input2"], cell_axis="col", compression=par["compression"], sparse=True, chunksize=1000
      )
      
      print("Transforming into adata")
      adata = create_joint_adata(adata1, adata2)
      adata = filter_joint_data_empty_cells(adata)
      
      adata.uns["dataset_id"] = par["id"]
      
      if par["test"]:
          print("Subsetting dataset")
          adata = subset_joint_data(adata)
          adata.uns["dataset_id"] = par["id"] + "_test"
      
      print("Writing adata to file")
      adata.write(par["output"], compression = "gzip")
      VIASHMAIN
      PATH="$VIASH_RESOURCES_DIR:\$PATH"
      
      python "\$tempscript"
      
      VIASHEOF
      

    dest: "scprep_csv"
    is_executable: true
  - type: "file"
    path: "../../utils/utils.py"
  - type: "file"
    text: |
        docker.enabled = true
        docker.runOptions = "-i -v ${baseDir}:${baseDir}"
        process.container = "dataintuitive/viash"
        params {
          output = "viash_no_value"
          scprep_csv__id = "citeseq_cbmc"
          scprep_csv__input1 = "https:/www.ncbi.nlm.nih.gov/geo/download/?acc=GSE100866&format=file&file=GSE100866%5FCBMC%5F8K%5F13AB%5F10X%2DRNA%5Fumi%2Ecsv%2Egz"
          scprep_csv__input2 = "https:/www.ncbi.nlm.nih.gov/geo/download/?acc=GSE100866&format=file&file=GSE100866%5FCBMC%5F8K%5F13AB%5F10X%2DADT%5Fumi%2Ecsv%2Egz"
          scprep_csv__compression = "gzip"
          scprep_csv__test = "no_default_value_configured"
          scprep_csv__output = "${params.output}"
          id = ""
          input = ""
          output = ""
          testScript = "test.py"
          testResources = [ "test.py" ]
          scprep_csv {
            name = "scprep_csv"
            container = "modality_alignment/datasets_scprep_csv"
            containerTag = "main_build"
            containerRegistry = "openpipeline"
            command = "scprep_csv"
            tests {
              isDefined = true
              testScript = "test.py"
              testResources = [ "test.py" ]
            }
            arguments {
              id {
                name = "id"
                otype = "--"
                required = false
                type = "string"
                direction = "Input"
                multiple = false
                multiple_sep = ":"
                value = "${params.scprep_csv__id}"
                dflt = "citeseq_cbmc"
                description = "The id of the output dataset id"
              }
              input1 {
                name = "input1"
                otype = "--"
                required = false
                type = "file"
                direction = "Input"
                multiple = false
                multiple_sep = ":"
                value = "${params.scprep_csv__input1}"
                dflt = "https:/www.ncbi.nlm.nih.gov/geo/download/?acc=GSE100866&format=file&file=GSE100866%5FCBMC%5F8K%5F13AB%5F10X%2DRNA%5Fumi%2Ecsv%2Egz"
                description = "Path or URL to the RNA counts as a gzipped csv file."
              }
              input2 {
                name = "input2"
                otype = "--"
                required = false
                type = "file"
                direction = "Input"
                multiple = false
                multiple_sep = ":"
                value = "${params.scprep_csv__input2}"
                dflt = "https:/www.ncbi.nlm.nih.gov/geo/download/?acc=GSE100866&format=file&file=GSE100866%5FCBMC%5F8K%5F13AB%5F10X%2DADT%5Fumi%2Ecsv%2Egz"
                description = "Path or URL to the ADT counts as a gzipped csv file."
              }
              compression {
                name = "compression"
                otype = "--"
                required = false
                type = "string"
                direction = "Input"
                multiple = false
                multiple_sep = ":"
                value = "${params.scprep_csv__compression}"
                dflt = "gzip"
                description = "For on-the-fly decompression of on-disk data. If 'infer' and filepath_or_buffer is path-like, then detect compression from the following extensions: '.gz', '.bz2', '.zip', or '.xz' (otherwise no decompression). If using 'zip', the ZIP file must contain only one data file to be read in. Set to None for no decompression."
              }
              test {
                name = "test"
                otype = "--"
                required = false
                type = "boolean_true"
                direction = "Input"
                multiple = false
                multiple_sep = ":"
                value = "${params.scprep_csv__test}"
                description = "Subset the dataset"
              }
              output {
                name = "output"
                otype = "--"
                required = true
                type = "file"
                direction = "Output"
                multiple = false
                multiple_sep = ":"
                value = "${params.scprep_csv__output}"
                dflt = "output.h5ad"
                description = "Output h5ad file containing both input matrices data"
              }
            }
          }
        }

    dest: "nextflow.config"
  - type: "file"
    text: |
      nextflow.enable.dsl=2
      
      params.test = false
      params.debug = false
      
      def checkParams(_params) {
        _params.arguments.collect{
          if (it.value == "viash_no_value") {
            println("[ERROR] option --${it.name} not specified in component scprep_csv")
            println("exiting now...")
              exit 1
          }
        }
      }
      
      
      def renderCLI(command, arguments) {
      
        def argumentsList = arguments.collect{ it ->
          (it.otype == "")
            ? "\'" + it.value + "\'"
            : (it.type == "boolean_true")
              ? it.otype + it.name
              : (it.value == "no_default_value_configured")
                ? ""
                : it.otype + it.name + " \'" + ((it.value in List && it.multiple) ? it.value.join(it.multiple_sep): it.value) + "\'"
        }
      
        def command_line = command + argumentsList
      
        return command_line.join(" ")
      }
      
      def effectiveContainer(processParams) {
        def _registry = params.containsKey("containerRegistry") ? params.containerRegistry : processParams.containerRegistry
        def _name = processParams.container
        def _tag = params.containsKey("containerTag") ? params.containerTag : processParams.containerTag
      
        return (_registry == "" ? "" : _registry + "/") + _name + ":" + _tag
      }
      
      // Convert the nextflow.config arguments list to a List instead of a LinkedHashMap
      // The rest of this main.nf script uses the Map form
      def argumentsAsList(_params) {
        def overrideArgs = _params.arguments.collect{ key, value -> value }
        def newParams = _params + [ "arguments" : overrideArgs ]
        return newParams
      }
      
      
      // Use the params map, create a hashmap of the filenames for output
      // output filename is <sample>.<method>.<arg_name>[.extension]
      def outFromIn(_params) {
      
        def id = _params.id
      
        _params
          .arguments
          .findAll{ it -> it.type == "file" && it.direction == "Output" }
          .collect{ it ->
            // If a default (dflt) attribute is present, strip the extension from the filename,
            // otherwise just use the option name as an extension.
            def extOrName = (it.dflt != null) ? it.dflt.split(/\./).last() : it.name
            // The output filename is <sample> . <modulename> . <extension>
            def newName =
              (id != "")
                ? id + "." + "scprep_csv" + "." + extOrName
                : "scprep_csv" + "." + extOrName
            it + [ value : newName ]
          }
      
      }
      
      // A process that filters out output from the output Map
      process filterOutput {
      
        input:
          tuple val(id), val(input), val(_params)
        output:
          tuple val(id), val(output), val(_params)
        when:
          input.keySet().contains("output")
        exec:
          output = input["output"]
      
      }
      
      def overrideIO(_params, inputs, outputs) {
      
        // `inputs` in fact can be one of:
        // - `String`,
        // - `List[String]`,
        // - `Map[String, String | List[String]]`
        // Please refer to the docs for more info
        def overrideArgs = _params.arguments.collect{ it ->
          if (it.type == "file") {
            if (it.direction == "Input") {
              (inputs in List || inputs in HashMap)
                ? (inputs in List)
                  ? it + [ "value" : inputs.join(it.multiple_sep)]
                  : (inputs[it.name] != null)
                    ? (inputs[it.name] in List)
                      ? it + [ "value" : inputs[it.name].join(it.multiple_sep)]
                      : it + [ "value" : inputs[it.name]]
                    : it
                : it + [ "value" : inputs ]
            } else {
              (outputs in List || outputs in HashMap)
                ? (outputs in List)
                  ? it + [ "value" : outputs.join(it.multiple_sep)]
                  : (outputs[it.name] != null)
                    ? (outputs[it.name] in List)
                      ? it + [ "value" : outputs[it.name].join(it.multiple_sep)]
                      : it + [ "value" : outputs[it.name]]
                    : it
                : it + [ "value" : outputs ]
            }
          } else {
            it
          }
        }
      
        def newParams = _params + [ "arguments" : overrideArgs ]
      
        return newParams
      
      }
      
      process scprep_csv_process {
      
      
        tag "${id}"
        echo { (params.debug == true) ? true : false }
        cache 'deep'
        stageInMode "symlink"
        container "${container}"
      
        input:
          tuple val(id), path(input), val(output), val(container), val(cli), val(_params)
        output:
          tuple val("${id}"), path(output), val(_params)
        script:
          if (params.test)
            """
            # Some useful stuff
            export NUMBA_CACHE_DIR=/tmp/numba-cache
            # Running the pre-hook when necessary
            # Adding NXF's `$moduleDir` to the path in order to resolve our own wrappers
            export PATH="./:${moduleDir}:\$PATH"
            ./${params.scprep_csv.tests.testScript} | tee $output
            """
          else
            """
            # Some useful stuff
            export NUMBA_CACHE_DIR=/tmp/numba-cache
            # Running the pre-hook when necessary
            # Adding NXF's `$moduleDir` to the path in order to resolve our own wrappers
            export PATH="${moduleDir}:\$PATH"
            $cli
            """
      }
      
      workflow scprep_csv {
      
        take:
        id_input_params_
      
        main:
      
        def key = "scprep_csv"
      
        def id_input_output_function_cli_params_ =
          id_input_params_.map{ id, input, _params ->
      
            // Start from the (global) params and overwrite with the (local) _params
            def defaultParams = params[key] ? params[key] : [:]
            def overrideParams = _params[key] ? _params[key] : [:]
            def updtParams = defaultParams + overrideParams
            // Convert to List[Map] for the arguments
            def newParams = argumentsAsList(updtParams) + [ "id" : id ]
      
            // Generate output filenames, out comes a Map
            def output = outFromIn(newParams)
      
            // The process expects Path or List[Path], Maps need to be converted
            def inputsForProcess =
              (input in HashMap)
                ? input.collect{ k, v -> v }.flatten()
                : input
            def outputsForProcess = output.collect{ it.value }
      
            // For our machinery, we convert Path -> String in the input
            def inputs =
              (input in List || input in HashMap)
                ? (input in List)
                  ? input.collect{ it.name }
                  : input.collectEntries{ k, v -> [ k, (v in List) ? v.collect{it.name} : v.name ] }
                : input.name
            outputs = output.collectEntries{ [(it.name): it.value] }
      
            def finalParams = overrideIO(newParams, inputs, outputs)
      
            checkParams(finalParams)
      
            new Tuple6(
              id,
              inputsForProcess,
              outputsForProcess,
              effectiveContainer(finalParams),
              renderCLI([finalParams.command], finalParams.arguments),
              finalParams
              )
          }
      
        result_ = scprep_csv_process(id_input_output_function_cli_params_) \
          | join(id_input_params_) \
          | map{ id, output, _params, input, original_params ->
              def parsedOutput = _params.arguments
                .findAll{ it.type == "file" && it.direction == "Output" }
                .withIndex()
                .collectEntries{ it, i ->
                  // with one entry, output is of type Path and array selections
                  // would select just one element from the path
                  [(it.name): (output in List) ? output[i] : output ]
                }
              new Tuple3(id, parsedOutput, original_params)
            }
      
        result_ \
          | filter { it[1].keySet().size() > 1 } \
          | view{
              ">> Be careful, multiple outputs from this component!"
          }
      
        emit:
        result_.flatMap{ it ->
          (it[1].keySet().size() > 1)
            ? it[1].collect{ k, el -> [ it[0], [ (k): el ], it[2] ] }
            : it[1].collect{ k, el -> [ it[0], el, it[2] ] }
        }
      }
      
      workflow {
      
        def id = params.id
        def _params = argumentsAsList(params.scprep_csv) + [ "id" : id ]
        def p = _params
          .arguments
          .findAll{ it.type == "file" && it.direction == "Input" }
          .collectEntries{ [(it.name): file(params[it.name]) ] }
      
        def ch_ = Channel.from("").map{ s -> new Tuple3(id, p, params)}
      
        result = scprep_csv(ch_)
        result.view{ it[1] }
      }
      
      // This workflow is not production-ready yet, we leave it in for future dev
      // TODO
      workflow test {
      
        take:
        rootDir
      
        main:
        params.test = true
        params.scprep_csv.output = "scprep_csv.log"
      
        Channel.from(rootDir) \
          | filter { params.scprep_csv.tests.isDefined } \
          | map{ p -> new Tuple3(
              "tests",
              params.scprep_csv.tests.testResources.collect{ file( p + it ) },
              params
          )} \
          | scprep_csv
      
        emit:
        scprep_csv.out
      }

    dest: "main.nf"
  description: "Create a modality alignment dataset from CSV using scprep."
  tests:
  - type: "python_script"
    path: "test.py"
    is_executable: true
  info: {}
platform:
  type: "nextflow"
  id: "nextflow"
  registry: "openpipeline"
  namespace_separator: "_"
  labels: []
platforms: []
info:
  config: "src/modality_alignment/datasets/scprep_csv/config.vsh.yaml"
  platform: "nextflow"
  output: "target/nextflow/modality_alignment/datasets/scprep_csv"
  executable: "target/nextflow/modality_alignment/datasets/scprep_csv/scprep_csv"
  viash_version: "0.5.0-rc2"
  git_commit: "a73bcd940759292415bc0ff591951a95e611175c"
  git_remote: "https://github.com/openproblems-bio/opsca-viash"
