type: file
example: "resources_test/spatially_variable_genes/10x_Visium_mouse_brain/solution.csv"
info:
  label: Solution
  summary: "CSV file with estimated spatial variability score for each gene."
  slots:
    - type: string
      name: feature_name
      description: feature name
      required: true
    - type: string
      name: gene_name
      description: gene names
      required: true
    - type: double
      name: spatial_var_score
      description: estimated spatial variability
      required: true