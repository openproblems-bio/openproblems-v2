type: file
example: "resources_test/cell_cell_communication_source_target/mouse_brain_atlas/prediction.h5ad"
info:
  label: "Prediction"
  summary: "The prediction file"
  slots:
    obs:
      - type: object
        name: ccc_pred
        description: Predicted interactions for the test cells.
        required: true
    uns:
      - type: string
        name: dataset_id
        description: "A unique identifier for the dataset"
        required: true
      - type: string
        name: normalization_id
        description: "Which normalization was used"
        required: true
      - type: string
        name: method_id
        description: "A unique identifier for the method"
        required: true
