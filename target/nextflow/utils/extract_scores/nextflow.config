  docker.enabled = true
  docker.runOptions = "-i -v ${baseDir}:${baseDir}"
  process.container = "dataintuitive/viash"
  params {
    extract_scores__input = "input.h5ad"
    extract_scores__output = "output.tsv"
    id = ""
    input = ""
    output = ""
    testScript = ""
    testResources = [  ]
    extract_scores {
      name = "extract_scores"
      container = "utils_extract_scores"
      containerTag = "main_build"
      containerRegistry = "openpipeline"
      command = "extract_scores"
      tests {
        isDefined = false
        testScript = "NA"
        testResources = [  ]
      }
      arguments {
        input {
          name = "input"
          otype = "--"
          required = false
          type = "file"
          direction = "Input"
          multiple = true
          multiple_sep = ":"
          value = "${params.extract_scores__input}"
          dflt = "input.h5ad"
          description = "Input h5ad files containing metadata and metrics in adata.uns"
        }
        output {
          name = "output"
          otype = "--"
          required = false
          type = "file"
          direction = "Output"
          multiple = false
          multiple_sep = ":"
          value = "${params.extract_scores__output}"
          dflt = "output.tsv"
          description = "Output tsv"
        }
      }
    }
  }